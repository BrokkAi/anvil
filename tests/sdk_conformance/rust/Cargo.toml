[package]
name = "brokk-anvil-sdk-conformance"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
brokk-anvil-sdk = { path = "../../../sdk/rust" }
tokio = { version = "1", features = ["macros", "rt-multi-thread", "time"] }
