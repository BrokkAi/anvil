{
  "description": "Deterministically recovered task contracts from historically successful trajectories; used for handoff/replay audits, never as solver prompt input.",
  "schema_version": "asgard-known-success-obligations/v1",
  "tasks": {
    "actionlint-action-pinning-lint": [
      "A per-path entry enables the rule even without a global `action-pinning` section.",
      "A ref that satisfies a stricter level (e.g., commit-sha) also satisfies any less strict requirement (e.g., semver, major-minor).",
      "A ref that satisfies semver also satisfies major-minor.",
      "Allowed and denied actions use `owner/repo` format.",
      "An `-action-pinning-level` CLI flag exists.",
      "Commit everything when done.",
      "Config validation rejects invalid level values.",
      "Config validation rejects malformed `owner/repo` entries in `allowed-actions`.",
      "Config validation rejects malformed `owner/repo` entries in `denied-actions`.",
      "Config validation rejects malformed `owner/repo` entries in both allowed and denied lists.",
      "Config validation rejects owners with slashes in `allowed-owners`.",
      "Config validation rejects owners with slashes in `denied-owners`.",
      "Config validation rejects owners with slashes.",
      "Denials take precedence over allowances.",
      "Docker refs starting with `docker://` are skipped (not flagged).",
      "Entries in allowed lists that are not also denied are not checked for pinning.",
      "Entries in denied lists are still checked for pinning (not unconditionally allowed).",
      "Entries in denied lists are still subject to pinning checks rather than unconditionally blocked.",
      "Error messages distinguish reusable workflows from step actions.",
      "For popular actions in the known-actions data, error suggestions reference the specific known version.",
      "Global and per-path allowed lists merge by union across matching configurations.",
      "Global and per-path denied lists merge by union across matching configurations.",
      "Level `commit-sha` requires full 40-character lowercase hex SHA.",
      "Level `major-minor` requires vMAJOR.MINOR format.",
      "Level `semver` requires vMAJOR.MINOR.PATCH format including prerelease.",
      "Local refs starting with `./` are skipped (not flagged).",
      "Owner matching is case-insensitive.",
      "Per-path overrides use the `action-pinning` key to override the pinning level.",
      "Setting `action-pinning: null` keeps the rule disabled.",
      "Setting `action-pinning: {}` enables the rule with defaults.",
      "The `-action-pinning-level` CLI flag enables the rule even when it would otherwise be disabled.",
      "The `-action-pinning-level` CLI flag overrides only the pinning level (not allow/deny lists).",
      "The `level` field accepts the value `commit-sha`.",
      "The `level` field accepts the value `major-minor`.",
      "The `level` field accepts the value `semver`.",
      "The config section has a `level` field.",
      "The config supports `allowed-actions` field.",
      "The config supports `allowed-owners` field.",
      "The config supports `denied-actions` field.",
      "The config supports `denied-owners` field.",
      "The default level when none is specified is `semver`.",
      "The error kind for the lint rule is exactly the string `action-pinning`.",
      "The error message for a job-level reusable workflow with a dynamic expression version ref indicates the ref is a dynamic expression that cannot be verified for pinning.",
      "The error message for a step-level action with a dynamic expression version ref indicates the ref is a dynamic expression that cannot be verified for pinning.",
      "The levels are ordered by increasing strictness: a ref satisfying a stricter level also satisfies any less strict requirement.",
      "The lint rule error kind string must be exactly `action-pinning`.",
      "The rule checks job-level reusable workflow `uses:` references for version pinning.",
      "The rule checks step-level action `uses:` references for version pinning.",
      "The rule is configured via an `action-pinning` config section.",
      "When only the version ref is a dynamic expression, flag it with an error indicating the ref is a dynamic expression that cannot be verified for pinning.",
      "When the action name itself is an expression, skip the reference entirely (no error).",
      "Work on this in a new branch from main."
    ],
    "happy-dom-abort-pending-body-reads": [
      "All work must be committed when done (the branch must contain commits covering the implementation).",
      "All work must be committed when done.",
      "Fully buffered `Response` bodies must remain readable after shutdown.",
      "Request and Response body reads that complete successfully before any shutdown trigger fires must remain unchanged \u2014 they must resolve normally with their intended value.",
      "Response bodies whose content has been fully buffered (all payload data loaded into internal buffers) before shutdown must remain readable after shutdown occurs.",
      "Scheduled timers (`setTimeout`, `setInterval`, etc.) associated with discarded page state must be cleared (cancelled and not executed) when the page state is discarded via shutdown or navigation.",
      "Scheduled timers (setTimeout, setInterval) that were registered on the page/window state being discarded must be cleared \u2014 they must not fire \u2014 when the page state is discarded via happyDOM.close(), page.close(), browser.close(), or a navigation that swaps out the active page state.",
      "Successful reads that complete without interruption must remain unchanged \u2014 their resolved values must be the same as if shutdown had never occurred.",
      "The implementation work must be done on a new branch created from main.",
      "When shutdown occurs through `browser.close()` while a `Request` or `Response` body read is in flight, the read must reject with a `DOMException` named `AbortError`.",
      "When shutdown occurs through `browser.close()` while multipart `formData()` parsing is in flight, the parse must reject with a `DOMException` named `AbortError`.",
      "When shutdown occurs through `happyDOM.close()` while a `Request` or `Response` body read is in flight, the read must reject with a `DOMException` named `AbortError`.",
      "When shutdown occurs through `happyDOM.close()` while multipart `formData()` parsing is in flight, the parse must reject with a `DOMException` named `AbortError`.",
      "When shutdown occurs through `page.close()` while a `Request` or `Response` body read is in flight, the read must reject with a `DOMException` named `AbortError`.",
      "When shutdown occurs through `page.close()` while multipart `formData()` parsing is in flight, the parse must reject with a `DOMException` named `AbortError`.",
      "When shutdown occurs through a navigation that swaps out the active page state while a `Request` or `Response` body read is in flight, the read must reject with a `DOMException` named `AbortError`.",
      "When shutdown occurs through a navigation that swaps out the active page state while multipart `formData()` parsing is in flight, the parse must reject with a `DOMException` named `AbortError`.",
      "When shutdown occurs via happyDOM.close(), page.close(), browser.close(), or a navigation that swaps out the active page state, any in-progress Request body read must reject with a DOMException whose name property is \"AbortError\".",
      "When shutdown occurs via happyDOM.close(), page.close(), browser.close(), or a navigation that swaps out the active page state, any in-progress Response body read must reject with a DOMException whose name property is \"AbortError\".",
      "When shutdown occurs via happyDOM.close(), page.close(), browser.close(), or a navigation that swaps out the active page state, in-progress multipart formData() parsing (reading and parsing multipart body content) must reject with a DOMException whose name property is \"AbortError\".",
      "Work must be done on a new branch from main.",
      "`requestAnimationFrame` callbacks associated with discarded page state must be cleared (cancelled and not executed) when the page state is discarded via shutdown or navigation.",
      "requestAnimationFrame callbacks that were registered on the page/window state being discarded must be cleared \u2014 they must not fire \u2014 when the page state is discarded via happyDOM.close(), page.close(), browser.close(), or a navigation that swaps out the active page state."
    ],
    "koota-entity-snapshot-rollback": [
      "All work is committed when done (no uncommitted changes at completion).",
      "In the `EntitySnapshot`, data traits are stored as a deep copy of the original trait data in the `traits` record.",
      "In the `EntitySnapshot`, relations with a store include `data` as a deep copy of the original relation data.",
      "In the `EntitySnapshot`, tag traits (traits with no data) are stored as `true` in the `traits` record.",
      "In the `EntitySnapshot`, the `relations` property is omitted entirely (not present as a key on the object) when the entity has no relations.",
      "The entity object has a `.rollback(registry, snapshot)` convenience method added to it.",
      "The entity object has a `.snapshot(registry)` convenience method added to it.",
      "The module's public API exports `createTraitRegistry`.",
      "The module's public API exports `diffEntitySnapshots`.",
      "The module's public API exports `diffWorldSnapshots`.",
      "The module's public API exports `rollbackEntity`.",
      "The module's public API exports `rollbackWorld`.",
      "The module's public API exports `snapshotEntity`.",
      "The module's public API exports `snapshotWorld`.",
      "The work is done on a new branch created from main.",
      "The world object has a `.rollback(registry, checkpoint)` convenience method added to it.",
      "The world object has a `.snapshot(registry)` convenience method added to it.",
      "`createTraitRegistry(...entries)` accepts rest arguments where each entry is a `[string, Trait | Relation]` tuple.",
      "`createTraitRegistry(...entries)` throws `Error` when any entry key is a duplicate of another entry key.",
      "`createTraitRegistry(...entries)` throws `Error` when any entry value is a duplicate Relation (by reference or identity) of another entry value.",
      "`createTraitRegistry(...entries)` throws `Error` when any entry value is a duplicate Trait (by reference or identity) of another entry value.",
      "`createTraitRegistry` when passed entries with keys `[\"a\", traitA]` and `[\"b\", traitB]` maps key `\"a\"` to `traitA` and key `\"b\"` to `traitB` \u2014 each tuple's first element pairs with its second element at construction.",
      "`diffEntitySnapshots(a, b)` has exactly two parameters in this order: a (before), b (after).",
      "`diffEntitySnapshots` classifies a trait present in `a` but absent in `b` into `removedTraits`, not `addedTraits` or `changedTraits`.",
      "`diffEntitySnapshots` classifies a trait present in `b` but absent in `a` into `addedTraits`, not `removedTraits` or `changedTraits`.",
      "`diffEntitySnapshots` classifies a trait present in both `a` and `b` but with shallowly unequal data into `changedTraits`, not `addedTraits` or `removedTraits`.",
      "`diffEntitySnapshots` compares trait data using shallow equality.",
      "`diffEntitySnapshots` returns `{ addedTraits: string[], removedTraits: string[], changedTraits: string[] }`.",
      "`diffEntitySnapshots` returns all three arrays (`addedTraits`, `removedTraits`, `changedTraits`) sorted ascending.",
      "`diffEntitySnapshots` throws `Error` if argument `a` is null or undefined.",
      "`diffEntitySnapshots` throws `Error` if argument `b` is null or undefined.",
      "`diffWorldSnapshots(before, after)` has exactly two parameters in this order: before, after.",
      "`diffWorldSnapshots` classifies an entity ID present in `after` but absent in `before` into `added`, not `removed` or `changed`.",
      "`diffWorldSnapshots` classifies an entity ID present in `before` but absent in `after` into `removed`, not `added` or `changed`.",
      "`diffWorldSnapshots` classifies an entity ID present in both `before` and `after` but with shallowly unequal trait/relation data into `changed`, not `added` or `removed`.",
      "`diffWorldSnapshots` compares trait and relation data using shallow equality.",
      "`diffWorldSnapshots` returns `{ added: number[], removed: number[], changed: number[] }`.",
      "`diffWorldSnapshots` returns all three arrays (`added`, `removed`, `changed`) sorted ascending.",
      "`diffWorldSnapshots` throws `Error` if `after` is null or undefined.",
      "`diffWorldSnapshots` throws `Error` if `after` lacks an `entities` array property.",
      "`diffWorldSnapshots` throws `Error` if `before` is null or undefined.",
      "`diffWorldSnapshots` throws `Error` if `before` lacks an `entities` array property.",
      "`diffWorldSnapshots` treats an entity with `relations: {}` (empty relations object) as equivalent to an entity that has no `relations` key on its snapshot.",
      "`diffWorldSnapshots` treats entities as equal when they have the same relation keys regardless of the order those keys appear in the snapshot's relations record.",
      "`diffWorldSnapshots` treats entities as equal when they have the same relation targets regardless of the order those targets appear in the array.",
      "`diffWorldSnapshots` treats entities as equal when they have the same trait keys regardless of the order those keys appear in the snapshot's traits record.",
      "`entity.rollback(registry, snapshot)` when called produces the same effect as calling `rollbackEntity(world, entity, registry, snapshot)`.",
      "`entity.snapshot(registry)` when called produces the same result as calling `snapshotEntity(world, entity, registry)` (returns an `EntitySnapshot`).",
      "`rollbackEntity(world, entity, registry, snapshot)` has exactly four parameters in this order: world, entity, registry, snapshot.",
      "`rollbackEntity` adds or updates traits and relations on the entity so they exactly match the snapshot.",
      "`rollbackEntity` removes traits and relations that the entity currently has but are not present in the snapshot.",
      "`rollbackEntity` throws `Error` if a relation target entity does not exist in the world.",
      "`rollbackEntity` throws `Error` if the entity is destroyed.",
      "`rollbackEntity` throws `Error` if the snapshot references a registry key that is unknown (not registered).",
      "`rollbackWorld(world, registry, checkpoint)` has exactly three parameters in this order: world, registry, checkpoint.",
      "`rollbackWorld` fully replaces the existing world state.",
      "`rollbackWorld` recreates entities using the same IDs as in the checkpoint.",
      "`rollbackWorld` throws `Error` if the checkpoint contains a relation whose target entity does not exist in the world (dangling relation target).",
      "`rollbackWorld` throws `Error` if the checkpoint references a registry key that is unknown (not registered).",
      "`snapshotEntity(world, entity, registry)` has exactly three parameters in this order: world, entity, registry.",
      "`snapshotEntity` returns an `EntitySnapshot` with shape `{ id: number, traits: Record<string, object | true>, relations?: Record<string, Array<{ targetId: number, data?: object }>> }`.",
      "`snapshotEntity` throws `Error` when the entity has a trait or relation whose key is not registered in the registry.",
      "`snapshotEntity` throws `Error` when the entity is destroyed.",
      "`snapshotWorld(world, registry)` has exactly two parameters in this order: world, registry.",
      "`snapshotWorld` excludes the internal world entity from the returned `entities` array.",
      "`snapshotWorld` returns `{ entities: EntitySnapshot[] }`.",
      "`world.rollback(registry, checkpoint)` when called produces the same effect as calling `rollbackWorld(world, registry, checkpoint)`.",
      "`world.snapshot(registry)` when called produces the same result as calling `snapshotWorld(world, registry)` (returns `{ entities: EntitySnapshot[] }` excluding internal world entity)."
    ],
    "narwhals-rolling-window-suite": [
      "A window (all methods) with fewer than min_samples non-null values in total (including at boundaries/short windows) produces null.",
      "A window with fewer than `min_samples` non-null values produces null.",
      "All methods follow the same validation, classification, and backend delegation patterns as the existing `rolling_sum`, `rolling_mean`, `rolling_std`, and `rolling_var` methods.",
      "DuckDB does not support `percentile_cont` as a windowed aggregate function; rolling_quantile with `.over()` is not available on DuckDB.",
      "Everything must be committed when done.",
      "For lazy backends, `rolling_max` operation requires `.over(order_by=...)`.",
      "For lazy backends, `rolling_median` operation requires `.over(order_by=...)`.",
      "For lazy backends, `rolling_min` operation requires `.over(order_by=...)`.",
      "For lazy backends, `rolling_quantile` operation requires `.over(order_by=...)`.",
      "For lazy backends, rolling operations require `.over(order_by=...)`.",
      "Null inputs are excluded from the window.",
      "The `Expr` namespace exposes `rolling_min`, `rolling_max`, `rolling_median`, `rolling_quantile` methods.",
      "The `Series` namespace exposes `rolling_min`, `rolling_max`, `rolling_median`, `rolling_quantile` methods.",
      "When `center=True`, the window is centered around the current observation.",
      "When `min_samples` is `None`, it defaults to `window_size`.",
      "When `min_samples` is explicitly provided, `rolling_max` uses that value as the minimum non-null count threshold rather than defaulting to `window_size`.",
      "When `min_samples` is explicitly provided, `rolling_median` uses that value as the minimum non-null count threshold rather than defaulting to `window_size`.",
      "When `min_samples` is explicitly provided, `rolling_min` uses that value as the minimum non-null count threshold rather than defaulting to `window_size`.",
      "When `min_samples` is explicitly provided, `rolling_quantile` uses that value as the minimum non-null count threshold rather than defaulting to `window_size`.",
      "Work must be done on a new branch from main.",
      "`rolling_max` computes the rolling maximum over a window of `window_size` observations.",
      "`rolling_max` follows the same parameter conventions as existing `rolling_sum`, `rolling_mean`, `rolling_std`, `rolling_var`.",
      "`rolling_max` has signature `rolling_max(window_size, *, min_samples=None, center=False)`.",
      "`rolling_max` with `center=False` (default) produces a trailing/right-aligned window.",
      "`rolling_max` with `center=True` produces correct windowing where the window is centered around the current observation.",
      "`rolling_max` with `min_samples` set to a value different from `window_size` respects that minimum sample count for non-null values.",
      "`rolling_median` computes the rolling median over a window of `window_size` observations.",
      "`rolling_median` follows the same parameter conventions as existing `rolling_sum`, `rolling_mean`, `rolling_std`, `rolling_var`.",
      "`rolling_median` has signature `rolling_median(window_size, *, min_samples=None, center=False)`.",
      "`rolling_median` with `center=False` (default) produces a trailing/right-aligned window.",
      "`rolling_median` with `center=True` produces correct windowing where the window is centered around the current observation.",
      "`rolling_median` with `min_samples` set to a value different from `window_size` respects that minimum sample count for non-null values.",
      "`rolling_min` computes the rolling minimum over a window of `window_size` observations.",
      "`rolling_min` follows the same parameter conventions as existing `rolling_sum`, `rolling_mean`, `rolling_std`, `rolling_var`.",
      "`rolling_min` has signature `rolling_min(window_size, *, min_samples=None, center=False)`.",
      "`rolling_min` with `center=False` (default) produces a trailing/right-aligned window.",
      "`rolling_min` with `center=True` produces correct windowing where the window is centered around the current observation.",
      "`rolling_min` with `min_samples` set to a value different from `window_size` respects that minimum sample count for non-null values.",
      "`rolling_quantile` computes the rolling quantile over a window of `window_size` observations.",
      "`rolling_quantile` follows the same parameter conventions as existing `rolling_sum`, `rolling_mean`, `rolling_std`, `rolling_var`.",
      "`rolling_quantile` has a keyword-only parameter `interpolation` with default value `'linear'`.",
      "`rolling_quantile` has a keyword-only parameter `quantile` that is required (no default value).",
      "`rolling_quantile` has signature `rolling_quantile(window_size, *, quantile, interpolation='linear', min_samples=None, center=False)`.",
      "`rolling_quantile` parameter `interpolation: str` must be one of: 'linear', 'lower', 'higher', 'nearest', 'midpoint'. Invalid values raise `ValueError` with message starting with `\"Interpolation must be one of\"`.",
      "`rolling_quantile` parameter `quantile: float` must be in [0, 1]. Out-of-range values raise `ValueError` with message starting with `\"Quantile must be between 0.0 and 1.0\"`.",
      "`rolling_quantile` with `center=False` (default) produces a trailing/right-aligned window.",
      "`rolling_quantile` with `center=True` produces correct windowing where the window is centered around the current observation.",
      "`rolling_quantile` with `min_samples` set to a value different from `window_size` respects that minimum sample count for non-null values.",
      "`rolling_quantile` with interpolation='higher' computes the quantile using higher interpolation.",
      "`rolling_quantile` with interpolation='linear' computes the quantile using linear interpolation.",
      "`rolling_quantile` with interpolation='lower' computes the quantile using lower interpolation.",
      "`rolling_quantile` with interpolation='midpoint' computes the quantile using midpoint interpolation.",
      "`rolling_quantile` with interpolation='nearest' computes the quantile using nearest interpolation."
    ],
    "ofetch-per-origin-circuit-breaker": [
      "A half-open probe keeps its slot for the full logical request, including internal retries",
      "A successful logical request resets consecutive failures to `0`",
      "Additional probes (beyond halfOpenMaxRequests) fail fast immediately",
      "Allow at most `halfOpenMaxRequests` concurrent probes per origin",
      "Circuit breaker allows recovery through deterministic half-open probes",
      "Circuit breaker applies to `$fetch`",
      "Circuit breaker applies to `createFetch({ fetch })`",
      "Circuit breaker applies to clients derived from `.create()`",
      "Circuit breaker is opt-in (not applied by default)",
      "Circuit breaker prevents repeated calls to unhealthy origins",
      "Circuit state is keyed by URL origin (not path)",
      "Clients created from the same parent via `.create()` must share circuit state",
      "Commit everything when done",
      "Count a circuit failure for body-read/stream-consumption errors (for example, reused-body read failures)",
      "Count a circuit failure for exceptions from `onRequestError`",
      "Count a circuit failure for exceptions from `onResponseError`",
      "Count a circuit failure for exceptions from `onResponse`",
      "Count a circuit failure for exceptions from `parseResponse`",
      "Count a circuit failure for network/fetch rejection",
      "Count a circuit failure for response parsing errors",
      "Count a circuit failure for response statuses listed in `failureStatusCodes`",
      "Do not increment failure count per retry attempt",
      "Hook ordering follows existing pre-fetch lifecycle; blocked requests are only required to skip underlying fetch, not pre-fetch hooks",
      "If `circuitBreaker` is omitted or falsey, do not apply circuit tracking or blocking",
      "If retries are exhausted and the logical request fails, record exactly one failure",
      "Listed status failures must still increment circuit failure count when `ignoreResponseError` is `true`",
      "Non-listed 4xx/5xx may still reject normally, but must not increment circuit failure count",
      "One external call is one logical request, even with internal retries",
      "Only statuses in `failureStatusCodes` are status-based circuit failures",
      "Origin keying must use the effective request after pre-fetch `onRequest` mutation and request URL rewriting",
      "Origin resolution must support request inputs as `Request`",
      "Origin resolution must support request inputs as `URL`",
      "Origin resolution must support request inputs as `string`",
      "Parse/hook failures are not retried by status-based retry logic",
      "Rejected non-listed statuses must not be treated as success: they must not close half-open state",
      "Rejected non-listed statuses must not be treated as success: they must not reset failure streaks",
      "Relative string requests must be keyed by the effective origin after `baseURL` resolution",
      "Request option `circuitBreaker` accepts `true`",
      "Request option `circuitBreaker` accepts an object with `cooldown`",
      "Request option `circuitBreaker` accepts an object with `threshold`",
      "Request option `circuitBreaker` accepts an object with optional `failureStatusCodes`",
      "Request option `circuitBreaker` accepts an object with optional `halfOpenMaxRequests`",
      "State `closed` exists in the state model",
      "State `half-open` exists in the state model",
      "State `open` exists in the state model",
      "Tests must run without network access",
      "Transition `closed` -> `open` when consecutive failures reach `threshold`",
      "Transition `half-open` -> `closed` on successful probe",
      "Transition `half-open` -> `open` on failed probe, restarting cooldown from that failure time",
      "Transition `open` -> `half-open` after `cooldown`",
      "Use `Date.now()` for cooldown and half-open gating so fake timers work deterministically",
      "When `circuitBreaker: true`, defaults are `cooldown = 30000`",
      "When `circuitBreaker: true`, defaults are `failureStatusCodes = [408, 409, 425, 429, 500, 502, 503, 504]`",
      "When `circuitBreaker: true`, defaults are `halfOpenMaxRequests = 1`",
      "When `circuitBreaker: true`, defaults are `threshold = 5`",
      "When circuit is open, do not call underlying `fetch`",
      "When circuit is open, include `Circuit breaker is open` in the error message",
      "When circuit is open, reject immediately",
      "When half-open quota is exceeded, do not call underlying `fetch`",
      "When half-open quota is exceeded, include `Circuit breaker is open` in the error message",
      "When half-open quota is exceeded, reject immediately",
      "Work on a new branch from main",
      "circuitBreaker option accepts `true` (boolean literal)",
      "circuitBreaker option object's `cooldown` is used for the open-to-half-open wait duration in milliseconds",
      "circuitBreaker option object's `threshold` is used for the closed-to-open transition count",
      "circuitBreaker option object's optional `failureStatusCodes` is used for status-based circuit failure detection",
      "circuitBreaker option object's optional `halfOpenMaxRequests` is used for max concurrent probe count"
    ],
    "query-persist-restored-query-state": [
      "A new public helper named createPersisterRestoreResult is exported from query-core.",
      "All work must be committed when done.",
      "Bulk restoration from fine-grained storage must preserve the same semantics when rebuilding the cache (the same guarantees as single-query restoration apply).",
      "Bulk restoration must reconcile persisted snapshots with queries that already exist in memory.",
      "If the live cache has newer data (higher data freshness timestamp) but the persisted snapshot has newer error metadata (higher error freshness timestamp), the restored query must keep the newer data while also adopting the newer error state so the result remains a refetch error.",
      "Newer data must not be discarded just because the other side (persisted snapshot or live cache) has the newer error timestamp.",
      "Restored observer results exposed by supported adapters must reflect the persisted failure count and timestamp metadata instead of recomputing fresh values during mount.",
      "Restoring from storage must not drop page params for infinite queries.",
      "Restoring from storage must not rewrite the query to a clean success state.",
      "Restoring from storage must not silently clear persisted errors.",
      "Restoring over an existing query must merge data freshness and error freshness independently instead of replacing the whole query state as a single unit.",
      "The function signature of createPersisterRestoreResult must have exactly two positional properties in its argument: data and state, where data and state are both present in the single object argument.",
      "The restore path must not trigger normal fetch success callbacks.",
      "The restored query must end in fetchStatus set to idle.",
      "The restored query must expose isRefetchError when data and error are both present in the persisted state.",
      "The restored query must preserve status including error states (e.g. status remains 'error' when the persisted state had error).",
      "The restored query must retain the provided failure counters from the persisted state.",
      "The restored query must retain the provided infinite-query pagination state from the persisted state.",
      "The restored query must retain the provided invalidation markers from the persisted state.",
      "The restored query must retain the provided timestamps from the persisted state.",
      "When a persister returns the restored snapshot marker from createPersisterRestoreResult, TanStack Query must adopt the provided state as the active query state instead of converting the result into a normal success fetch.",
      "Work must be done on a new branch from main.",
      "createPersisterRestoreResult must accept an object with the shape { data, state } as its single argument (non-curried).",
      "createPersisterRestoreResult must return a value that can be returned from the persister option used by prefetchQuery and query observers to indicate that a persisted snapshot was restored instead of freshly fetched."
    ],
    "returns-validated-error-accumulation": [
      "A container type called Validated exists with two concrete subtypes Valid and Invalid.",
      "A container type named Validated exists with two concrete subtypes named Valid and Invalid.",
      "A pointfree bind_validated function exists in the pointfree package.",
      "A pointfree bind_validated function must be added and exported from the pointfree package.",
      "A result_to_validated converter function exists in the converters module.",
      "A validated_to_result converter function exists in the converters module.",
      "Add a validated decorator that catches exceptions and returns Invalid.",
      "Add result_to_validated converter function to the converters module.",
      "Add validated_to_result converter function to the converters module.",
      "All work must be committed when done.",
      "Commit everything when done.",
      "Create a new interface extending FailableN directly, with its own from_failure classmethod and custom short-circuit law specs for map, bind, and apply on failure values.",
      "Invalid has a from_failure classmethod that wraps a single error into a 1-tuple.",
      "Invalid must store its errors as an immutable tuple.",
      "Invalid must support structural pattern matching via __match_args__.",
      "Invalid stores its errors as an immutable tuple.",
      "No changes to iterables.py are needed; Fold.collect works automatically through apply.",
      "The alt method on Invalid must apply the provided function to each individual error element in the tuple, returning a new Invalid with the mapped results.",
      "The bind method must still short-circuit.",
      "The bind method on Invalid must short-circuit (not apply the provided function).",
      "The combine classmethod takes two Validated values and a binary function and produces a single Validated using applicative combination.",
      "The combine_n classmethod takes a tuple of N Validated containers and an N-ary function, accumulating all errors if any are failures.",
      "The from_failure classmethod must wrap a single error into a 1-tuple so accumulation works uniformly.",
      "The from_validated classmethod must return the same instance it receives (identity, not equality).",
      "The from_validated classmethod must return the same instance it receives.",
      "The new ValidatedLikeN interface has custom short-circuit law specs for apply on failure values.",
      "The new ValidatedLikeN interface has custom short-circuit law specs for bind on failure values.",
      "The new ValidatedLikeN interface has custom short-circuit law specs for map on failure values.",
      "The new ValidatedLikeN interface has its own from_failure classmethod.",
      "The pointfree bind_validated function is exported from returns/pointfree/__init__.py.",
      "The swap method must turn Invalid(errs) into Valid(errs).",
      "The swap method must turn Valid(x) into Invalid((x,)).",
      "The swap method on Invalid must turn Invalid(errs) into Valid(errs).",
      "The swap method on Valid must turn Valid(x) into Invalid((x,)).",
      "The validated decorator catches exceptions and returns Invalid when the wrapped function raises.",
      "The validated decorator catches exceptions and returns Invalid.",
      "The validated decorator must preserve the wrapped function's name.",
      "The validated decorator supports specifying exception types via an exceptions parameter.",
      "Update returns/contrib/hypothesis/containers.py to register ValidatedLikeN with from_failure strategy generation.",
      "Update returns/methods/cond.py to add a ValidatedLikeN dispatch branch before the container_type.empty fallback.",
      "Update returns/pointfree/__init__.py to export bind_validated.",
      "Valid and Invalid must support structural pattern matching via __match_args__.",
      "Valid must support structural pattern matching via __match_args__.",
      "Validated has a bind_validated method.",
      "Validated has a combine classmethod that takes two Validated values and a binary function and produces a single Validated using applicative combination.",
      "Validated has a combine classmethod.",
      "Validated has a combine_n classmethod that takes a tuple of N Validated containers and an N-ary function.",
      "Validated has a combine_n classmethod.",
      "Validated has a from_result classmethod that converts a Result into a Validated.",
      "Validated has a from_result classmethod.",
      "Validated must inherit standard container behavior including do-notation.",
      "Validated must inherit standard container behavior including equality.",
      "Validated must inherit standard container behavior including failure.",
      "Validated must inherit standard container behavior including from_value.",
      "Validated must inherit standard container behavior including repr.",
      "Validated must inherit standard container behavior including unwrap.",
      "Validated must inherit standard container behavior including value_or.",
      "Validated must integrate into the library's container interface hierarchy.",
      "ValidatedLikeN does NOT extend DiverseFailableN.",
      "ValidatedLikeN extends FailableN directly.",
      "When apply combines two Invalid containers, the resulting error tuple must be self's errors concatenated with the other's errors, preserving stable left-to-right order.",
      "When validating multiple independent inputs, all errors are collected rather than stopping at the first failure.",
      "Work must be done on a new branch from main.",
      "Work on a new branch from main.",
      "alt on Invalid returns a new Invalid (not the same instance) with the mapped results.",
      "combine classmethod produces a single Validated using applicative combination.",
      "combine's first Validated argument's unwrapped value reaches the first parameter of the binary function and the second Validated argument's value reaches the second parameter.",
      "combine_n accumulates all errors if any of the containers are failures.",
      "combine_n preserves the positional correspondence: the unwrapped value of the i-th element in the tuple becomes the i-th argument to the N-ary function.",
      "from_result converts Failure's error into Invalid by wrapping the error in a 1-tuple.",
      "from_result converts Success into Valid.",
      "from_result converts a Failure Result into an Invalid, wrapping the Failure's error in a 1-tuple.",
      "from_result converts a Success Result into a Valid.",
      "from_result wraps Result's Failure error value into a 1-tuple to become Invalid \u2014 the tuple contains the error, not the error directly.",
      "swap on Invalid(errs) returns Valid(errs) \u2014 the error tuple itself becomes the Valid value, not a 1-tuple wrapping it."
    ],
    "sql-formatter-bigquery-pipe-formatting": [
      "AGGREGATE and EXTEND are promoted to reserved clauses after the pipe operator (|>), receiving the same parse-node treatment as reserved clauses like WHERE or SELECT",
      "AGGREGATE with an optional nested GROUP BY sub-clause formats GROUP BY with its own indentation level, one level deeper than the AGGREGATE body",
      "All work is committed when finished",
      "Clause body indentation follows the same indentation pattern the formatter already uses for that clause type in traditional queries.",
      "Clauses that the existing formatter treats as indented clauses (`WHERE`, `SELECT`, `ORDER BY`, `AGGREGATE`, `EXTEND`, `SET`, `DROP`) place their body on a new indented line after the keyword.",
      "Clauses that the existing formatter treats as one-line clauses (`LIMIT`, `JOIN` and its variants, `AS`) keep their content on the same line as the keyword.",
      "Each AGGREGATE clause body starts on a new indented line after the keyword, and if a GROUP BY sub-clause is present it has its own additional indentation level.",
      "Each `|>` resets indentation to base indentation.",
      "Each pipe operator |> resets the indentation to base level for the line containing the operator and the following clause keyword",
      "Each subsequent `|>` step occupies its own line at base indentation.",
      "Each subsequent |> step after the initial FROM occupies its own line at base indentation",
      "Everything must be committed when done.",
      "For a mixed input containing both a pipe query and a traditional query, formatting the pipe query does not alter the formatting of the traditional query.",
      "For a mixed input containing both a traditional query and a pipe query, formatting the traditional query does not alter the formatting of the pipe query.",
      "For a one-line clause (LIMIT, JOIN and its variants (CROSS JOIN, INNER JOIN, LEFT JOIN, RIGHT JOIN, FULL JOIN, etc.), AS) in a pipe step, the clause content stays on the same line as the keyword.",
      "For a pipe query where `keywordCase` is set to a non-default case, all pipe keywords (including pipe-exclusive ones) are cased according to that setting.",
      "For a pipe query with multiple `|>` steps, each step's clause keyword and body are positioned correctly on their respective lines, with each `|>` resetting to base indentation.",
      "For an indented clause (WHERE, SELECT, ORDER BY, AGGREGATE, EXTEND, SET, DROP) in a pipe step, the clause body appears on a line after the keyword line, indented one level deeper.",
      "GROUP BY within AGGREGATE nests as a sub-clause with its own indentation level, one level deeper than the surrounding AGGREGATE body",
      "In a pipe query, the clause AGGREGATE places its body on a new indented line after the keyword (indented-clause behavior)",
      "In a pipe query, the clause AS keeps its content on the same line as the keyword (one-line-clause behavior)",
      "In a pipe query, the clause DROP places its body on a new indented line after the keyword (indented-clause behavior)",
      "In a pipe query, the clause EXTEND places its body on a new indented line after the keyword (indented-clause behavior)",
      "In a pipe query, the clause JOIN and its variants (LEFT JOIN, RIGHT JOIN, INNER JOIN, CROSS JOIN, etc.) keep their content on the same line as the keyword (one-line-clause behavior)",
      "In a pipe query, the clause LIMIT keeps its content on the same line as the keyword (one-line-clause behavior)",
      "In a pipe query, the clause ORDER BY places its body on a new indented line after the keyword (indented-clause behavior)",
      "In a pipe query, the clause SELECT places its body on a new indented line after the keyword (indented-clause behavior)",
      "In a pipe query, the clause SET places its body on a new indented line after the keyword (indented-clause behavior)",
      "In a pipe query, the clause WHERE places its body on a new indented line after the keyword (indented-clause behavior)",
      "Mixed pipe and traditional statements format independently from each other.",
      "Mixed pipe and traditional statements within the same input format independently, each following its own formatting rules without interference",
      "Pipe clauses produce structured parse nodes (AST nodes specific to pipe syntax, not reusing traditional clause nodes)",
      "Pipe clauses produce structured parse nodes distinct from traditional query parse nodes.",
      "Pipe queries enclosed in parentheses are formatted as nested subqueries.",
      "Pipe queries format starting with standalone FROM (no preceding SELECT or other clause before FROM)",
      "Pipe queries start with a standalone `FROM` clause (no preceding SELECT or WITH).",
      "Pipe query statements use `|>` operators to chain transformations instead of nested clauses.",
      "Pipe-exclusive keyword `AGGREGATE` must carry the correct classification as a pipe clause keyword (not a traditional SQL function).",
      "Pipe-exclusive keyword `AS` must carry the correct classification as a pipe clause keyword.",
      "Pipe-exclusive keyword `DROP` must carry the correct classification as a pipe clause keyword.",
      "Pipe-exclusive keyword `EXTEND` must carry the correct classification as a pipe clause keyword (not a traditional SQL function or identifier).",
      "Pipe-exclusive keyword `SET` must carry the correct classification as a pipe clause keyword.",
      "Semicolons (;) attach after the final pipe step, not after intermediate pipe steps or on a separate line",
      "Semicolons attach after the final pipe step of a pipe query.",
      "The clause body starts on the next line after the line containing the pipe operator and keyword, indented one level deeper than the pipe-operator line",
      "The clause body starts on the next line after the pipe operator and clause keyword, indented one level deeper.",
      "The indentation pattern for each pipe clause body follows the same indentation pattern the formatter already uses for that clause type in traditional (non-pipe) queries",
      "The pipe operator `|>` and its clause keyword share the same line.",
      "The pipe operator |> and the clause keyword following it share the same line",
      "The semicolon is placed after the final `|>` step, not after the standalone `FROM` or any intermediate step.",
      "The standalone `FROM` at the start of a pipe query is not preceded by `SELECT`, `WITH`, or any other clause that would begin a traditional query.",
      "The tokenizer recognizes |>, as a distinct token type and does not split it into bitwise OR (|) followed by greater-than (>)",
      "Traditional (non-pipe) BigQuery formatting remains unchanged; the formatter produces the same output for traditional queries as before pipe-awareness was added",
      "Traditional BigQuery formatting remains unchanged when format is applied to non-pipe queries.",
      "When a pipe query appears inside parentheses it is formatted as a subquery, preserving the pipe formatting rules within the parentheses",
      "Work is done on a new branch created from main",
      "Work must be done on a new branch from main.",
      "`AGGREGATE` and `EXTEND` are promoted to reserved clauses when they appear after `|>`.",
      "`AGGREGATE` in a pipe step has an optional nested `GROUP BY` sub-clause requiring its own indentation level.",
      "`AS` is a pipe-exclusive clause for naming intermediates.",
      "`DROP` is a pipe-exclusive clause for removing columns.",
      "`EXTEND` is a pipe-exclusive clause for computed columns.",
      "`GROUP BY` nested within `AGGREGATE` must carry the correct classification as a sub-clause keyword.",
      "`GROUP BY` within an `AGGREGATE` pipe step nests as a sub-clause with its own indentation.",
      "`SET` is a pipe-exclusive clause for replacing values.",
      "`keywordCase` setting governs all pipe keywords including pipe-exclusive ones (`AGGREGATE`, `EXTEND`, `SET`, `DROP`, `AS`, `GROUP BY` within AGGREGATE).",
      "`|>` must tokenize as a distinct token type, not as bitwise `|` followed by `>`.",
      "keywordCase governs all pipe keywords, including pipe-exclusive ones (AGGREGATE, EXTEND, SET, DROP, AS)"
    ],
    "true-myth-iterable-collection-combinators": [
      "A module named `maybe` must export `sequence`.",
      "A module named `maybe` must export `traverse`.",
      "A module named `maybe` must export `zipWith`.",
      "A module named `maybe` must export `zip`.",
      "A module named `result` must export `sequence`.",
      "A module named `result` must export `traverse`.",
      "A module named `result` must export `zipWith`.",
      "A module named `result` must export `zip`.",
      "A module named `task` must export `sequence`.",
      "A module named `task` must export `traverse`.",
      "A module named `task` must export `zipWith`.",
      "A module named `task` must export `zip`.",
      "A module named `toolbelt` must export `sequenceMaybeAsResult`.",
      "A module named `toolbelt` must export `traverseMaybeAsResult`.",
      "A module named `toolbelt` must export `zipMaybeAsResult`.",
      "All code must be committed when done.",
      "Each toolbelt function (`sequenceMaybeAsResult`, `traverseMaybeAsResult`, `zipMaybeAsResult`) must have a curried form `fn(errValue)` that returns a function taking the remaining arguments.",
      "Each toolbelt function (`sequenceMaybeAsResult`, `traverseMaybeAsResult`, `zipMaybeAsResult`) takes a caller-supplied `errValue` that converts `Nothing` into `Err`.",
      "Each toolbelt function has a curried form fn(errValue) returning a function that takes the remaining arguments",
      "Each toolbelt function must have a curried form `fn(errValue)` returning a function that takes the remaining arguments.",
      "Everything is committed when done",
      "Everything must be committed when done.",
      "For a rejected task, the async iterator must yield `Err`.",
      "For a resolved task, the async iterator must yield `Ok`.",
      "Maybe implements [Symbol.iterator]",
      "On `maybe` and `result`, `sequence` and `traverse` must stop advancing the iterator immediately after the first failure.",
      "On `maybe`, `sequence` must accept any `Iterable` and stop advancing the iterator immediately after the first `Nothing`.",
      "On `maybe`, `sequence` must accept any `Iterable`.",
      "On `maybe`, `traverse` must accept any `Iterable` and stop advancing the iterator immediately after the first `Nothing`.",
      "On `maybe`, `traverse` must accept any `Iterable`.",
      "On `maybe`, `traverse` must also have a curried form `traverse(fn)` that returns `(items) => result`.",
      "On `maybe`, `traverse` must work when called with the non-curried signature `traverse(items, fn)`.",
      "On `maybe`, `traverse`'s single-argument curried form `traverse(fn)` returns a function `(items) => result` where result is the appropriate container type (e.g., Maybe).",
      "On `result`, `sequence` must accept any `Iterable` and stop advancing the iterator immediately after the first `Err`.",
      "On `result`, `sequence` must accept any `Iterable`.",
      "On `result`, `traverse` must accept any `Iterable` and stop advancing the iterator immediately after the first `Err`.",
      "On `result`, `traverse` must accept any `Iterable`.",
      "On `result`, `traverse` must also have a curried form `traverse(fn)` that returns `(items) => result`.",
      "On `result`, `traverse` must work when called with the non-curried signature `traverse(items, fn)`.",
      "On `result`, `traverse`'s single-argument curried form `traverse(fn)` returns a function `(items) => result` where result is the appropriate container type (e.g., Result).",
      "On maybe, sequence and traverse accept any Iterable",
      "On maybe, sequence and traverse stop advancing the iterator immediately after the first failure",
      "On result, sequence and traverse accept any Iterable",
      "On result, sequence and traverse stop advancing the iterator immediately after the first failure",
      "Repository is clean (no uncommitted changes) when done",
      "Result implements [Symbol.iterator]",
      "Task implements [Symbol.asyncIterator]",
      "Task's [Symbol.asyncIterator] yields exactly one Result",
      "The async iterator on `Task` must yield exactly one `Result`.",
      "The async iterator on a rejected Task must yield exactly one `Result`: `Err` containing the rejection reason.",
      "The async iterator on a resolved Task must yield exactly one `Result`: `Ok` containing the resolved value.",
      "The module named `maybe` must export `compact`.",
      "The module named `maybe` must export `filterMap`.",
      "The module named `maybe` must export `firstJust`.",
      "The module named `result` must export `partition`.",
      "The module named `task` must export `retryN`.",
      "The module named `task` must export `tapRejected`.",
      "The module named `task` must export `tap`.",
      "The module named `task` must export `traverseSerial`.",
      "The single yielded Result is Err for a rejected task",
      "The single yielded Result is Ok for a resolved task",
      "Work is done on a new branch from main",
      "Work must be done on a new branch from `main`.",
      "Work must be done on a new branch from main.",
      "`Maybe` must implement `[Symbol.iterator]`.",
      "`Result` must implement `[Symbol.iterator]`.",
      "`Task` must implement `[Symbol.asyncIterator]`.",
      "`compact` must be added to `maybe`.",
      "`compact` on `maybe` must drop `Nothing` values silently \u2014 only values from `Just` entries appear in the output.",
      "`compact` on `maybe` must drop failures (Nothings) silently.",
      "`compact` on `maybe` \u2014 each element whose value passes through does so in the original order with correct position, and each Nothing is dropped without replacement.",
      "`filterMap` must also have a curried form `filterMap(fn)` returning `(items) => result`.",
      "`filterMap` must be added to `maybe`.",
      "`filterMap` must work with the non-curried signature `filterMap(items, fn)`.",
      "`filterMap` on `maybe` must drop `Nothing` results from `fn` silently \u2014 only values from `Just` entries appear in the output.",
      "`filterMap` on `maybe` must drop failures (Nothings) silently.",
      "`filterMap` on `maybe` must have a curried form `filterMap(fn)` returning `(items) => result`.",
      "`filterMap` on `maybe` must have a non-curried signature `filterMap(items, fn)`.",
      "`filterMap`'s curried form `filterMap(fn)` returns `(items) => result` where result is an array of values (filtered).",
      "`firstJust(maybes)` must be added to `maybe`.",
      "`firstJust(maybes)` must return the first `Just` in the array (not a later one) when multiple `Just` values exist.",
      "`firstJust(maybes)` must return the first `Just` in the array, or `Nothing` if no `Just` exists.",
      "`firstJust` must return `Nothing` if no `Just` exists in the array.",
      "`firstJust` must return the first `Just` in the array.",
      "`partition` must be added to `result`.",
      "`partition` must split results into `[oks, errs]` \u2014 a tuple/array of two arrays.",
      "`partition` on `result` must split an iterable of `Result` into `[oks, errs]` where `oks` contains all `Ok` values and `errs` contains all `Err` values.",
      "`partition` on `result` \u2014 each Ok value must appear in the oks array in original encounter order, and each Err value must appear in the errs array in original encounter order.",
      "`retryN(n, fn)` must be added to `task`.",
      "`retryN(n, fn)` must not retry when the task resolves \u2014 the resolved value is returned immediately on the first success.",
      "`retryN(n, fn)` must retry the task-producing function `fn` up to `n` additional times when the task rejects, for a total of at most `n+1` attempts.",
      "`retryN` must retry the task-producing function up to `n` additional times on rejection.",
      "`sequenceMaybeAsResult` must be added to `toolbelt`.",
      "`sequenceMaybeAsResult` must convert each `Nothing` in the input into an `Err(errValue)` using the caller-supplied `errValue`.",
      "`sequence` must be added to `maybe`.",
      "`sequence` must be added to `result`.",
      "`sequence` must be added to `task`.",
      "`tap(task, fn)` must be added to `task`.",
      "`tap(task, fn)` on `task` must execute the side-effect `fn` when the task resolves and pass the original resolved value through unchanged as the result.",
      "`tapRejected(task, fn)` must be added to `task`.",
      "`tapRejected(task, fn)` on `task` must execute the side-effect `fn` when the task rejects and pass the original rejection reason through unchanged as the result.",
      "`tapRejected` must have a curried form `tapRejected(fn)` returning `(task) => result`.",
      "`tapRejected` must have a non-curried signature `tapRejected(task, fn)`.",
      "`tapRejected` on `task` must pass the value through unchanged (side effect only).",
      "`tapRejected`'s curried form `tapRejected(fn)` returns `(task) => result` where result is a task that resolves/rejects with the original error/value.",
      "`tap` must have a curried form `tap(fn)` returning `(task) => result`.",
      "`tap` must have a non-curried signature `tap(task, fn)`.",
      "`tap` on `task` must pass the value through unchanged (side effect only).",
      "`tap`'s curried form `tap(fn)` returns `(task) => result` where result is a task that resolves to the original value.",
      "`traverseMaybeAsResult` must be added to `toolbelt`.",
      "`traverseMaybeAsResult` must convert each `Nothing` returned by `fn` into an `Err(errValue)` using the caller-supplied `errValue`.",
      "`traverseMaybeAsResult` must have a non-curried signature `traverseMaybeAsResult(errValue, items, fn)`.",
      "`traverseMaybeAsResult` must have the non-curried signature `traverseMaybeAsResult(errValue, items, fn)`.",
      "`traverseSerial` must also have a curried form `traverseSerial(fn)` returning `(items) => result`.",
      "`traverseSerial` must be added to `task`.",
      "`traverseSerial` must have a curried form `traverseSerial(fn)` returning `(items) => result`.",
      "`traverseSerial` must have a non-curried signature `traverseSerial(items, fn)`.",
      "`traverseSerial` must work with the non-curried signature `traverseSerial(items, fn)`.",
      "`traverseSerial` on `task` must execute sequentially (one after another, not in parallel).",
      "`traverseSerial` on `task` must process items sequentially (one at a time, waiting for each to complete before starting the next).",
      "`traverseSerial` on `task` must stop on the first rejection (not continue processing remaining items).",
      "`traverseSerial` on `task` must stop on the first rejection.",
      "`traverseSerial`'s curried form `traverseSerial(fn)` returns `(items) => result` where result is the appropriate task container.",
      "`traverse` must be added to `maybe`.",
      "`traverse` must be added to `result`.",
      "`traverse` must be added to `task`.",
      "`traverse` must have a curried form `traverse(fn)` that returns a function `(items) => result` on `maybe`, `result`, and `task`.",
      "`traverse` must have a non-curried signature `traverse(items, fn)` on `maybe`, `result`, and `task`.",
      "`zipMaybeAsResult` must be added to `toolbelt`.",
      "`zipMaybeAsResult` must convert each `Nothing` in the input into an `Err(errValue)` using the caller-supplied `errValue`.",
      "`zipWith` must be added to `maybe`.",
      "`zipWith` must be added to `result`.",
      "`zipWith` must be added to `task`.",
      "`zipWith` must have the signature `zipWith(a, b, fn)` \u2014 data arguments first, combiner function last \u2014 on `maybe`, `result`, and `task`.",
      "`zipWith` on `maybe`, `result`, and `task` must have the signature `(a, b, fn)` \u2014 data arguments first, combiner function last.",
      "`zipWith` on `maybe`, `result`, and `task` \u2014 each input's value must reach its correct output position; the combiner function must receive `a` as its first argument and `b` as its second argument.",
      "`zip` must be added to `maybe`.",
      "`zip` must be added to `result`.",
      "`zip` must be added to `task`.",
      "`zip` on `maybe`, `result`, and `task` \u2014 the first input's value must occupy the first position of the pair and the second input's value must occupy the second position.",
      "compact drops failures (Nothing) silently",
      "compact is added to maybe",
      "compact on maybe preserves all Just values in iteration order, dropping Nothings silently",
      "filterMap drops failures (Nothing returned by fn) silently",
      "filterMap has the curried form filterMap(fn) returning (items) => result",
      "filterMap has the non-curried signature filterMap(items, fn)",
      "filterMap has the non-curried signature filterMap(items, fn) where items is first and fn is second \u2014 each input's value reaches its correct output position",
      "filterMap is added to maybe",
      "filterMap on maybe applies fn to each item, collects Just results in iteration order, dropping Nothings silently",
      "firstJust is added to maybe",
      "firstJust(maybes) returns Nothing if no Just exists in the array",
      "firstJust(maybes) returns the first Just in the array",
      "partition is added to result",
      "partition on result splits into [oks, errs] where oks appear first in the tuple and errs appear second",
      "partition splits the input into [oks, errs] where oks contains all Ok values and errs contains all Err values",
      "retryN has the signature retryN(n, fn) where n (number of additional retries) is first and fn (task-producing function) is second \u2014 each input's value reaches its correct output position",
      "retryN is added to task",
      "retryN(n, fn) retries the task-producing function up to n additional times when the task rejects",
      "sequence is added to maybe",
      "sequence is added to result",
      "sequence is added to task",
      "sequenceMaybeAsResult is added to toolbelt",
      "sequenceMaybeAsResult, traverseMaybeAsResult, and zipMaybeAsResult each take a caller-supplied errValue that converts Nothing into Err",
      "tap has the curried form tap(fn) returning (task) => result",
      "tap has the non-curried signature tap(task, fn)",
      "tap has the non-curried signature tap(task, fn) where task is first and fn is second \u2014 each input's value reaches its correct output position",
      "tap is added to task",
      "tap(task, fn) passes the resolved value through unchanged (the returned task resolves with the same value)",
      "tapRejected has the curried form tapRejected(fn) returning (task) => result",
      "tapRejected has the non-curried signature tapRejected(task, fn)",
      "tapRejected has the non-curried signature tapRejected(task, fn) where task is first and fn is second \u2014 each input's value reaches its correct output position",
      "tapRejected is added to task",
      "tapRejected(task, fn) passes the rejected reason through unchanged (the returned task rejects with the same reason)",
      "traverse has the non-curried signature traverse(items, fn)",
      "traverse has the single-argument curried form traverse(fn) returning (items) => result",
      "traverse is added to maybe",
      "traverse is added to result",
      "traverse is added to task",
      "traverse on maybe has the non-curried signature traverse(items, fn) where items is first and fn is second \u2014 each input's value reaches its correct output position",
      "traverse on result has the non-curried signature traverse(items, fn) where items is first and fn is second \u2014 each input's value reaches its correct output position",
      "traverse on task has the non-curried signature traverse(items, fn) where items is first and fn is second \u2014 each input's value reaches its correct output position",
      "traverseMaybeAsResult has the non-curried signature traverseMaybeAsResult(errValue, items, fn)",
      "traverseMaybeAsResult has the non-curried signature traverseMaybeAsResult(errValue, items, fn) where errValue is first, items is second, and fn is third \u2014 each input's value reaches its correct output position",
      "traverseMaybeAsResult is added to toolbelt",
      "traverseSerial has the curried form traverseSerial(fn) returning (items) => result",
      "traverseSerial has the non-curried signature traverseSerial(items, fn)",
      "traverseSerial has the non-curried signature traverseSerial(items, fn) where items is first and fn is second \u2014 each input's value reaches its correct output position",
      "traverseSerial is added to task",
      "traverseSerial processes items sequentially (one after another, not concurrently)",
      "traverseSerial stops on first rejection (does not process remaining items after a rejection)",
      "zip is added to maybe",
      "zip is added to result",
      "zip is added to task",
      "zipMaybeAsResult is added to toolbelt",
      "zipWith is added to maybe",
      "zipWith is added to result",
      "zipWith is added to task",
      "zipWith takes (a, b, fn) where a is first, b is second, and fn (combiner) is last \u2014 each input's value reaches its correct output position",
      "zipWith takes (a, b, fn) with data arguments first and combiner function last"
    ],
    "ts-pattern-match-each": [
      "Add a new top-level function `matchEach` that is a named export from the package entry point.",
      "Commit everything when you are done.",
      "Each clause maintains independent selection state.",
      "Each clause's handler receives the original input value (not a narrowed or transformed form).",
      "Each input's value reaches its correct output position: when a clause at declaration position N matches, its handler's result appears at index N in the results array, not at a position determined by input value ordering.",
      "Every `.with()` call must accept patterns against the original input type, not the progressively narrowed remainder.",
      "Exhaustiveness tracking should still narrow the internal type so `.exhaustive()` can verify all cases are handled.",
      "Multiple tap points can be stacked.",
      "Named selections from one clause must not leak into another clause's handler.",
      "Results are returned in the order clauses were declared.",
      "Selections via `P.select()` must produce independent results across multiple calls of any compiled function.",
      "Tap callbacks also execute inside compiled functions produced by `.toExhaustiveFunction()`.",
      "Tap callbacks also execute inside compiled functions produced by `.toFunction()`.",
      "Tap callbacks also execute inside compiled functions produced by `.toPartialFunction()`.",
      "Tap does not affect the results array.",
      "The task states `match` short-circuits on the first matching pattern; this is a distinguishing description. `matchEach` must not short-circuit \u2014 it must continue evaluating all remaining patterns even after finding a match.",
      "When `.exhaustive()` fallback is provided and no pattern matches at runtime, the fallback is called and its result is returned in a single-element array instead of throwing.",
      "When the expression is evaluated, each tap point calls its callback once per result that has been collected up to that point in declaration order.",
      "Work on this in a new branch from main.",
      "`.exhaustive()` accepts an optional fallback handler function.",
      "`.exhaustive()` enforces compile-time exhaustiveness: it should be a type error if not all input cases are handled.",
      "`.exhaustive()` returns an array of all matching handler results.",
      "`.exhaustive()` throws `NonExhaustiveError` if nothing matched.",
      "`.exhaustive()` with fallback: when fallback is provided and some patterns match, matching results are returned (fallback is not included).",
      "`.narrow()` updates both the internal tracking type and the input type for subsequent calls to exclude handled cases.",
      "`.otherwise()` never throws.",
      "`.otherwise(handler)` returns `[handler(value)]` when no patterns matched.",
      "`.otherwise(handler)` returns the array of all matching results when at least one pattern matched (the default handler is not included when patterns match).",
      "`.run()` returns an array of all matching handler results.",
      "`.run()` throws `NonExhaustiveError` if nothing matched.",
      "`.tap(callback)` registers a side-effect callback and returns a new `matchEach` for continued chaining.",
      "`.tap(callback)` returns a new `matchEach` object, not the same instance.",
      "`.toExhaustiveFunction()` compiles the registered clauses into a reusable `(input) => output[]` function.",
      "`.toExhaustiveFunction()` does not accept a fallback \u2014 only `.exhaustive()` accepts an optional fallback.",
      "`.toExhaustiveFunction()` enforces compile-time exhaustiveness: it should be a type error if not all input cases are handled.",
      "`.toExhaustiveFunction()` throws `NonExhaustiveError` if no pattern matches at runtime.",
      "`.toFunction()` compiles the registered clauses into a reusable `(input) => output[]` function.",
      "`.toFunction()` throws `NonExhaustiveError` if no pattern matches at runtime.",
      "`.toPartialFunction()` compiles into a function that returns `output[] | undefined`.",
      "`.toPartialFunction()` never throws.",
      "`.toPartialFunction()` returns `undefined` when no patterns match instead of throwing.",
      "`.with()` multi-pattern overload: when multiple patterns are passed to a single `.with()`, each input value reaches the handler if any of the patterns match (multi-pattern acts as OR).",
      "`.with()` overloads include single pattern, multi-pattern, and guard variants, all accepting patterns against the original input type.",
      "`NonExhaustiveError` is thrown (imported or defined as the exact error type) when the task specifies throwing on no-match.",
      "`matchEach` can be called without a value argument using explicit type parameters to build a reusable compiled matcher.",
      "`matchEach` collects every matching handler's result into an array.",
      "`matchEach` evaluates ALL registered patterns against the input (unlike `match` which short-circuits on the first matching pattern).",
      "`matchEach` must expose `.narrow()` as part of its builder API.",
      "`matchEach` must expose `.returnType()` as part of its builder API.",
      "`matchEach` must expose `.when()` as part of its builder API.",
      "`matchEach` must expose the same builder API as `match`, including all `.with()` overloads (single pattern, multi-pattern, and guard variants)."
    ],
    "wazero-multi-module-snapshots": [
      "A Coordinator struct exists with exported methods NewCoordinator, CaptureSnapshot, CaptureIncremental, RestoreSnapshot.",
      "A global registry is provided with functions Register(name string, c *Coordinator), Get(name string) (*Coordinator, bool), and Unregister(name string).",
      "All Coordinator methods (NewCoordinator, CaptureSnapshot, CaptureIncremental, RestoreSnapshot) are safe for concurrent use.",
      "All Coordinator methods (NewCoordinator, CaptureSnapshot, CaptureIncremental, RestoreSnapshot) must be safe for concurrent use.",
      "All Coordinator methods must be safe for concurrent use.",
      "All work is committed when done.",
      "All work must be committed when done.",
      "CaptureIncremental accepts a baseline Snapshot (which may itself be incremental) plus variadic modules and returns (Snapshot, error).",
      "CaptureIncremental accepts a baseline Snapshot plus variadic api.Module arguments \u2014 signature CaptureIncremental(baseline Snapshot, modules ...api.Module) (Snapshot, error).",
      "CaptureIncremental accepts a baseline Snapshot that may itself be incremental (incremental-on-incremental).",
      "CaptureIncremental returns an error containing \"baseline snapshot is nil\" when baseline argument is nil.",
      "CaptureIncremental returns an error containing \"module count mismatch\" when variadic module count differs from the number of modules in the baseline snapshot.",
      "CaptureIncremental returns an error containing 'baseline snapshot is nil' for nil baseline.",
      "CaptureIncremental returns an error containing 'module count mismatch' when module count differs from baseline.",
      "CaptureIncremental when the number of modules differs from the baseline's module count returns an error whose string contains 'module count mismatch'.",
      "CaptureIncremental with a nil baseline returns an error whose string contains 'baseline snapshot is nil'.",
      "CaptureSnapshot accepts variadic api.Module arguments and returns (Snapshot, error).",
      "CaptureSnapshot accepts variadic api.Module arguments \u2014 signature CaptureSnapshot(modules ...api.Module) (Snapshot, error).",
      "CaptureSnapshot returns an error containing \"module closed\" when any passed module is nil or closed.",
      "CaptureSnapshot returns an error containing \"no modules\" when passed zero modules.",
      "CaptureSnapshot returns an error containing 'module closed' for nil or closed modules.",
      "CaptureSnapshot returns an error containing 'no modules' for empty input.",
      "CaptureSnapshot with a nil Module or a closed Module returns an error whose string contains 'module closed'.",
      "CaptureSnapshot with no modules returns an error whose string contains 'no modules'.",
      "Chain has method Head() returning the last snapshot or nil if empty.",
      "Chain has method Head() that returns last Snapshot or nil.",
      "Chain has method Len() returning the count of snapshots.",
      "Chain has method Len() that returns count.",
      "Chain has method Push(snap Snapshot) that appends the snapshot.",
      "Chain has method Push(snap) that appends a Snapshot.",
      "Chain has method Snapshots() returning a copy of the internal slice, oldest-first.",
      "Chain has method Snapshots() that returns a copy oldest-first.",
      "Chain type exists with constructor NewChain() *Chain.",
      "Chain type exists with exported constructor NewChain() *Chain.",
      "Chain type exists; NewChain() *Chain creates an empty one.",
      "Chain type is exported (capital C).",
      "Chain.Head returns nil for an empty chain.",
      "Chain.Len returns 0 for an empty chain.",
      "Chain.Snapshots() returns a copy of the internal slice (oldest-first order), not the original slice.",
      "Compare groups DiffEntry results by module in the same capture order that Data() returns modules.",
      "Compare performs byte-level diff of fully reconstructed memory, grouped by module in capture order, offsets sorted ascending within each module.",
      "Compare reports byte-level diffs grouped by module in capture order, with offsets sorted ascending within each module.",
      "Compare returns byte-level diff of fully reconstructed memory, grouped by module in capture order, with offsets sorted ascending within each module.",
      "Compare returns byte-level diffs of fully reconstructed memory, grouped by module in capture order.",
      "Compare returns diffs with offsets sorted ascending within each module group.",
      "CompressedData for full snapshots is the gzip of Data() concatenated in capture order.",
      "CompressedData() for a full snapshot is the gzip-compressed form of Data() concatenated in capture order.",
      "CompressedData() for a full snapshot returns gzip-compressed Data() concatenated in capture order.",
      "CompressedData() for an incremental snapshot compresses to strictly smaller output than the baseline's CompressedData.",
      "CompressedData() for an incremental snapshot returns output that is strictly smaller (in bytes) than the baseline's CompressedData.",
      "Context helper GetCoordinator(ctx) exists and returns (*Coordinator, bool) where bool is false if absent.",
      "Context helper GetCoordinator(ctx) exists and returns nil when no Coordinator is present.",
      "Context helper WithCoordinator(ctx, c) exists.",
      "Coordinator struct exists in package experimental/snapshot.",
      "Coordinator struct exists with exported field-less constructor NewCoordinator().",
      "Coordinator struct is exported (capital C).",
      "Coordinator.Version() numbers start at 1 for the first capture on a given Coordinator.",
      "Data() on an incremental Snapshot returns fully reconstructed memory (not a delta or partial data).",
      "Data() on an incremental Snapshot returns fully reconstructed memory (not just the delta).",
      "Data() on an incremental snapshot returns fully reconstructed memory \u2014 the baseline merged with incremental changes.",
      "Data() on an incremental snapshot returns fully reconstructed memory.",
      "Data() on any Snapshot returns fully reconstructed memory per module (a [][]byte slice with one element per module).",
      "DiffEntry is a struct with fields Offset uint32, OldValue byte, NewValue byte.",
      "DiffEntry is a struct with fields Offset uint32, OldValue byte, and NewValue byte.",
      "DiffEntry struct is exported (capital D).",
      "DiffEntry.Offset is uint32, OldValue is the byte in the receiver, NewValue is the byte in the argument snapshot.",
      "Each Coordinator instance has its own version counter starting at 1; separate Coordinators do not share version sequences.",
      "Each call to Data() returns an independent deep copy; mutating the returned slice does not affect the Snapshot.",
      "Each call to Data() returns an independent deep copy; mutation of one returned slice does not affect subsequent Data() calls.",
      "Each call to Tags() returns an independent deep copy; mutating the returned map does not affect the Snapshot.",
      "Each call to Tags() returns an independent deep copy; mutation of one returned map does not affect subsequent Tags() calls.",
      "Each input module's memory reaches its correct positional output in Data() for capture operations.",
      "ErrorCode applied to the error from an insufficient restore target returns 'insufficient_memory'.",
      "ErrorCode(err) returns \"insufficient_memory\" for errors caused by insufficient restore target size.",
      "Everything is committed when done.",
      "For insufficient restore target size, ErrorCode(err) returns 'insufficient_memory'.",
      "For restore matching, first try reference identity (same pointer as captured), then fall back to positional order when the restore count equals the snapshot module count.",
      "Function MarshalSnapshot(snap Snapshot) ([]byte, error) exists.",
      "Function MarshalSnapshot(snap) ([]byte, error) exists.",
      "Function Summarize(snap Snapshot) returns SnapshotSummary.",
      "Function Summarize(snap) returns SnapshotSummary.",
      "Function UnmarshalSnapshot(data []byte) (Snapshot, error) exists.",
      "Function UnmarshalSnapshot(data) (Snapshot, error) exists.",
      "GetCoordinator returns nil (or zero *Coordinator, false) when no coordinator is present in the context.",
      "GetCoordinator(ctx) returns nil if no Coordinator is present in the context.",
      "Global function Get(name string) (*Coordinator, bool) exists.",
      "Global function Register(name string, c *Coordinator) exists.",
      "Global function Unregister(name string) exists.",
      "Head() returns the last snapshot pushed, or nil for an empty chain.",
      "Incremental snapshots compress to strictly smaller output than the baseline's CompressedData.",
      "Len() returns the count of snapshots in the Chain.",
      "Marshal and Unmarshal round-trip: UnmarshalSnapshot(MarshalSnapshot(snap)) produces a snapshot with identical Data(), Version(), and Tags() to snap.",
      "Marshal/Unmarshal round-trips: UnmarshalSnapshot(MarshalSnapshot(snap)) returns a Snapshot with identical Data(), Version(), and Tags() to snap.",
      "MarshalSnapshot encodes fully reconstructed Data(), Version(), and Tags() portably (cross-architecture, cross-version).",
      "MarshalSnapshot encodes fully reconstructed Data(), Version(), and Tags() portably.",
      "MarshalSnapshot errors on failure.",
      "MarshalSnapshot returns an error on failure.",
      "MarshalSnapshot returns error on failure.",
      "MarshalSnapshot(snap) ([]byte, error) exists.",
      "MarshalSnapshot/UnmarshalSnapshot round-trips: UnmarshalSnapshot(MarshalSnapshot(snap)) yields a Snapshot whose Data(), Version(), and Tags() equal the original's.",
      "NewCoordinator() returns *Coordinator.",
      "NewCoordinator() returns a *Coordinator.",
      "NewSnapshotCoordinator delegates to snapshot.NewCoordinator().",
      "NewSnapshotCoordinator() *snapshot.Coordinator exists in package experimental, delegating to snapshot.NewCoordinator().",
      "NewSnapshotCoordinator() *snapshot.Coordinator is added to package experimental, delegating to snapshot.NewCoordinator().",
      "Package experimental has exported function NewSnapshotCoordinator that returns *snapshot.Coordinator.",
      "Package for the core types is experimental/snapshot (implied by 'Build this system in the experimental/snapshot package').",
      "Package must be placed at experimental/snapshot.",
      "Push(snap) appends a snapshot to the Chain.",
      "Register replaces any existing entry with the same name.",
      "Register replaces any existing entry.",
      "RestoreSnapshot accepts a Snapshot plus variadic api.Module arguments \u2014 signature RestoreSnapshot(snap Snapshot, modules ...api.Module) error.",
      "RestoreSnapshot accepts a Snapshot plus variadic modules and returns an error.",
      "RestoreSnapshot falls back to positional order matching only when the number of restore modules equals the snapshot module count.",
      "RestoreSnapshot falls back to positional order only when the number of restore modules equals the snapshot module count.",
      "RestoreSnapshot first tries reference identity (same pointer as captured) to match restore modules to captured modules.",
      "RestoreSnapshot first tries reference identity (same pointer as captured) to match restore modules to snapshot modules.",
      "RestoreSnapshot restores memory into modules matched by reference identity first; the pointer comparison is against the exact *api.Module pointers captured.",
      "RestoreSnapshot returns an error containing \"incompatible module\" when passed more modules than were captured in the snapshot.",
      "RestoreSnapshot returns an error containing 'incompatible module' when more modules are passed to RestoreSnapshot than were captured.",
      "RestoreSnapshot returns nil even if no modules matched (when fewer modules are provided).",
      "RestoreSnapshot returns nil even when no modules matched (all unmatched and silently skipped).",
      "RestoreSnapshot when fewer modules are provided matches each module by pointer identity only, falls back to nothing for non-matching modules, and silently skips unmatched modules.",
      "RestoreSnapshot when more modules are passed than were captured returns an error whose string contains 'incompatible module'.",
      "SetTag method on Snapshot interface has signature SetTag(key, value string).",
      "SetTag(key, value string) modifies the snapshot's tags but does not affect already-returned deep copies from Tags().",
      "Snapshot interface has method Compare(other Snapshot) []DiffEntry.",
      "Snapshot interface has method CompressedData() []byte.",
      "Snapshot interface has method SetTag(key, value string).",
      "Snapshot interface has method Tags() map[string]string.",
      "Snapshot interface has method Version() uint64.",
      "Snapshot interface is exported (capital S).",
      "Snapshot interface methods: Data() [][]byte, CompressedData() []byte, Version() uint64, Tags() map[string]string, SetTag(key, value string), Compare(other Snapshot) []DiffEntry \u2014 exact signatures as stated.",
      "Snapshot is a Go interface with exactly these methods: Data() [][]byte, CompressedData() []byte, Version() uint64, Tags() map[string]string, SetTag(key, value string), Compare(other Snapshot) []DiffEntry.",
      "Snapshot is a Go interface with method Data() [][]byte.",
      "Snapshot must be a Go interface with method Compare(other Snapshot) []DiffEntry.",
      "Snapshot must be a Go interface with method CompressedData() []byte.",
      "Snapshot must be a Go interface with method Data() [][]byte.",
      "Snapshot must be a Go interface with method SetTag(key, value string).",
      "Snapshot must be a Go interface with method Tags() map[string]string.",
      "Snapshot must be a Go interface with method Version() uint64.",
      "Snapshot.Compare returns []DiffEntry.",
      "SnapshotSummary is a struct with fields TotalModules int, TotalBytes uint64, ModifiedBytes uint64, Version uint64.",
      "SnapshotSummary struct exists with fields TotalModules int, TotalBytes uint64, ModifiedBytes uint64, Version uint64.",
      "SnapshotSummary struct has fields TotalModules int, TotalBytes uint64, ModifiedBytes uint64, Version uint64.",
      "SnapshotSummary type is exported (capital S).",
      "Snapshots are immutable after capture -- Data() returns independent deep copies across multiple calls.",
      "Snapshots are immutable after capture -- Tags() returns independent deep copies across multiple calls.",
      "Snapshots are immutable after capture: each call to Data() must return independent deep copies.",
      "Snapshots are immutable after capture: each call to Tags() must return independent deep copies.",
      "Snapshots() returns a copy of the chain's snapshots in oldest-first order.",
      "Snapshots() returns a copy: mutating the returned slice does not affect the Chain's internal state.",
      "Summarize function is exported (capital S).",
      "Summarize returns ModifiedBytes as zero for full snapshots.",
      "Summarize returns ModifiedBytes equal to changed-byte count for incremental snapshots.",
      "Summarize returns TotalBytes equal to total reconstructed bytes across all modules.",
      "Summarize returns TotalModules equal to the snapshot's module count.",
      "Summarize returns Version equal to snap.Version().",
      "Summarize sets ModifiedBytes to the count of changed bytes for incremental snapshots.",
      "Summarize sets ModifiedBytes to zero for full snapshots.",
      "Summarize sets TotalBytes to the total reconstructed bytes across all modules.",
      "Summarize sets TotalModules to the module count of the snapshot.",
      "Summarize sets Version to snap.Version().",
      "Summarize(snap) returns a SnapshotSummary.",
      "Summarize(snap): ModifiedBytes equals changed-byte count for incrementals.",
      "Summarize(snap): ModifiedBytes is zero for full snapshots.",
      "Summarize(snap): TotalBytes is total reconstructed bytes.",
      "Summarize(snap): TotalModules is module count.",
      "Summarize(snap): Version matches snap.Version().",
      "The global registry (Register, Get, Unregister) is safe for concurrent use.",
      "The global registry functions (Register, Get, Unregister) are package-level exported functions in the experimental/snapshot package.",
      "The registry is safe for concurrent use (Register, Get, Unregister called from multiple goroutines).",
      "The registry must be safe for concurrent use.",
      "There is a function ErrorCode(err) that returns a string.",
      "There is a function GetCoordinator(ctx) that returns (*Coordinator, bool) or equivalent; nil if absent.",
      "There is a function WithCoordinator(ctx, c) that attaches a Coordinator to a context.",
      "There is a global named coordinator registry with Get(name string) (*Coordinator, bool).",
      "There is a global named coordinator registry with Register(name string, c *Coordinator).",
      "There is a global named coordinator registry with Unregister(name string).",
      "UnmarshalSnapshot decodes data and returns a full snapshot (not incremental).",
      "UnmarshalSnapshot decodes data produced by MarshalSnapshot and returns a full snapshot (not incremental).",
      "UnmarshalSnapshot errors on failure.",
      "UnmarshalSnapshot returns a full Snapshot (not incremental) from marshalled data.",
      "UnmarshalSnapshot returns a full snapshot (not incremental) regardless of what was marshaled.",
      "UnmarshalSnapshot returns an error on failure.",
      "UnmarshalSnapshot returns error on failure.",
      "UnmarshalSnapshot(data) (Snapshot, error) exists.",
      "UnmarshalSnapshot(data) where data was produced by MarshalSnapshot from a full snapshot returns a full Snapshot (not incremental), meaning Data() returns fully reconstructed bytes, not deltas.",
      "UnmarshalSnapshot(data) where data was produced by MarshalSnapshot from an incremental snapshot returns a full Snapshot (not incremental) \u2014 the incremental character is lost.",
      "Unmatched restore modules in RestoreSnapshot are silently skipped and RestoreSnapshot returns nil even if no modules matched.",
      "Version method on Snapshot interface returns uint64.",
      "Version starts at 1 after NewCoordinator and before any capture call.",
      "Version() returns a monotonically increasing value per Coordinator instance, starting at 1.",
      "Version() returns uint64 that is monotonically increasing per Coordinator, starting at 1.",
      "Versions increase monotonically per Coordinator, starting at 1.",
      "Versions increase monotonically without gaps across both CaptureSnapshot and CaptureIncremental calls on the same Coordinator.",
      "Versions increase without gaps across both CaptureSnapshot and CaptureIncremental calls on the same Coordinator.",
      "When fewer modules are provided to RestoreSnapshot than captured, each restore module is matched by identity only.",
      "When fewer modules are provided to RestoreSnapshot than were captured, each is matched by identity only; unmatched modules are silently skipped.",
      "When restore count equals snapshot module count and no pointer identities match, restoration proceeds positionally: the nth restore module receives the nth snapshot module's memory.",
      "Work is done on a new branch from main.",
      "Work is on a new branch from main.",
      "Work must be done on a new branch from main."
    ]
  }
}
